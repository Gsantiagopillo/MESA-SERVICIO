const DOMAIN = "http://localhost/mesa-servicio/",
  API = `${DOMAIN}api/v1.0/`,
  REPORTES = `${API}reportes/`,
  LOGIN = `${API}login/`,
  USUARIOS = `${API}usuarios/`,
  EMPRESAS = `${API}empresas/`,
  CLIENTES = `${API}clientes/`,
  IMPRESORAS = `${API}impresoras/`,
  KEY_SESSION = "ETI20231001";

export default {
  DOMAIN,
  API,
  REPORTES,
  LOGIN,
  USUARIOS,
  EMPRESAS,
  CLIENTES,
  IMPRESORAS,
  KEY_SESSION,
};
